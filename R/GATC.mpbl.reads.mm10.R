#' GRanges object with all GATC fragments of mm10, plus mappable reads.
#'
#' The reads are 39 bp
#'
#' @format A GRanges object; \code{granges} specify GATC fragments.
#' \code{mcols} has three integer columns with mappable read counts for
#' forward/reverse/both strands.
"GATC.mpbl.reads.mm10"
